import { ReplaceTagsPipe } from './replace-tags.pipe';

describe('ReplaceTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
